import * as React from 'react';

export interface IDeveloperProps {
}

export default class Developer extends React.Component<IDeveloperProps> {
  public render() {
    return (
      <div>
        Works, from dev page
      </div>
    );
  }
}
